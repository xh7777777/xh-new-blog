# 站点对外访问域名（用于 canonical / openGraph / sitemap）
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# 开发环境默认直连后端 http://localhost:3000
# 生产环境默认自动走 ${NEXT_PUBLIC_SITE_URL}/api
# 如需手动覆盖可设置：
# NEXT_PUBLIC_API_BASE_URL=/api
# SERVER_API_BASE_URL=https://your-domain.com/api
